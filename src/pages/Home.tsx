import BookForm from "../components/BookForm";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <main>
      <Hero />
      <BookForm />
    </main>
  );
};

export default Home;
